<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siigo.www.work_list">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.NewListActivity" />
        <activity android:name=".UI.TaskActivity" />
        <activity android:name=".UI.ListOfListsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".UI.TasksListActivity" />
        <activity android:name=".UI.NewAccountActivity" >
</activity>
        <activity android:name=".UI.StartActivity" >

        </activity>
        <activity android:name=".UI.LoginActivity"></activity>
    </application>

</manifest>